steps:
  - name: ":lambda: Test"
    command: npm run test
    agents:
      queue: elastic
    plugins:
      docker-compose:
        run: node

  - wait

  - name: ":lambda: Deploy"
    command: './buildkite/scripts/deploy.bash'
    agents:
      queue: elastic
    artifact_paths: "build.zip"
