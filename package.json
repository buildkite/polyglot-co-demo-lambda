{
  "devDependencies": {
    "mocha": "2",
    "fetch-mock": "4"
  },
  "scripts": {
  	"test": "mocha functions/**",
    "build": "apex build > build.zip",
    "deploy": "apex deploy --env-file env.json"
  }
}
